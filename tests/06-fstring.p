a = 2
b = 3

# combining strings
println(join([ "product: ", a * b , " sum product of squares: ", (a *a) + (b * b) ]))

# the same as an f-string
println("product: {a * b} sum product of squares: { (a *a) + (b * b) }")
